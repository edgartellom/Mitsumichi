import React from "react";

const Title_Label = ({ pageTitle }) => {
  return (
    <div>
      <h1 className="text-[#ffffff] text-[42px] font-bold">{pageTitle}</h1>
    </div>
  );
};

export default Title_Label;
